// Styles for /lib/components/data/cpu.jsx component
export const cpuStyles = /* css */ `
.cpu {
  color: var(--yellow);
  background-color: var(--minor);
}
.cpu--high {
  color: var(--foreground);
  background-color: var(--red);
}
.cpu--graph {
  min-width: 90px;
  padding: 0;
}
.cpu__usage {
  color: var(--foreground);
  padding: 3px;
  text-align: right;
}
`;
